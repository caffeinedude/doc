# Git
## 安装
## 基本配置
```
git config --global user.name "snoredude"
git config --global user.email "caffeine.dude.go@gmail.com"
```
--global 是全局级别的配置, 此外, 还有仓库级别的配置 --local, 以及系统级别的配置 --system  
--local 优先级更高, --system 优先级最低  

## 代理
```
git config --global --list
git config --local --list
git config --global http.proxy 127.0.0.1:8087
git config --global http.sslVerify false
git config --global --unset http.proxy
git config --global --unset http.sslVerify
```
http.sslVerify false 禁用 Git 的 SSL 验证

## 配置文件
```
git config --global --edit
git config --local --edit
git config --system --edit
```

## 代码提交
```
git add -A 或者 git add filename
git commit -m "your note"
git push origin master
git status
```

## 代码同步 ( fork 代码和原仓库代码同步 )
```
原仓库：https://github.com/cncamp/golang.git
fork
clone 到本地
在项目目录下，查看远程仓库路径
git remote -v
origin  https://github.com/snoredude/golang.git (fetch)
origin  https://github.com/snoredude/golang.git (push)
只显示2行内容，说明该项目还未设置upstream （上游代码库）
设置upstream
git remote add upstream https://github.com/cncamp/golang.git
检查是否成功
git remote -v
origin  https://github.com/snoredude/golang.git (fetch)
origin  https://github.com/snoredude/golang.git (push)
upstream        https://github.com/cncamp/golang.git (fetch)
upstream        https://github.com/cncamp/golang.git (push)
检查本地代码是否修改
git status
如果本地有修改，需要先从本地仓库推送到GitHub仓库。
抓取源仓库的更新
git fetch upstream
* [new branch]      master     -> upstream/master
上游仓库的更新（commit）会本存储为本地的分支 upstream/master
切换分支
切换到需要更新合并的分支
git checkout master
如果本身就位于该分支，不需要切换
执行合并
git merge upstream/master
如有代码冲突，需要解决。
推送到Gibhub
git push origin maste
```

### 其他
```
git status
git branch -vv
git branch -v
git branch -rv
git branch -av
git checkout test
git add .
git add -A
git add filename
git commit
git branch test
git branch fix test
git checkout -b newbranch
git branch -d fix
git fetch remote
git pull remote test
git push remote名称 test
查看全局的配置信息
查看某个仓库下的配置信息
查看当前仓库的变更情况
查看所有分支的最后一个提交信息（当前代码所在分支会被高亮）
列出所有本地分支
列出所有远端分支
列出所有本地分支 + 远端分支
切换到 test 分支
把当前目录下及子目录下的所有变更都加入到暂存区
把仓库内所有变更都加入到暂存区
把指定文件加入到暂存区
把暂存区的修改保存到本地仓库 
基于当前分支创建新的 test 分支
基于 test 分支创建新的 fix 分支
创建分支并切换到该分支
删除指定分支
拉取远端的所有分支和变更  
拉取远端的某个分支的变更，然后 merge 到本地分支
推送本地分支到远端
```


# Git

## 常用命令
查看当前代码所在分支：`git branch -vv`  
列出所有本地分支：`git branch -v`  
列出所有远端分支：`git branch -rv`  
列出所有本地分支 + 远端分支：`git branch -av`
