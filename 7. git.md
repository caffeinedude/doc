# 7. Git
## 简介
Git 是 代码托管 和 分布式版本控制 工具
## 7.1. 安装
## 7.2. 基本配置
```
git config --global user.name "snoredude"
git config --global user.email "caffeine.dude.go@gmail.com"
```
--global 是全局级别的配置, 此外, 还有仓库级别的配置 --local, 以及系统级别的配置 --system  
--local 优先级更高, --system 优先级最低  

## 代理
```
git config --global --list
git config --local --list
git config --global http.proxy 127.0.0.1:8087
git config --global http.sslVerify false
git config --global --unset http.proxy
git config --global --unset http.sslVerify
```
http.sslVerify false 禁用 Git 的 SSL 验证

## 配置文件
```
git config --global --edit
git config --local --edit
git config --system --edit
```

## 代码提交
```
git add -A 或者 git add filename
git commit -m "your note"
git push origin master
git status
```

## 代码同步 ( fork 代码和原仓库代码同步 )
```
原仓库：https://github.com/cncamp/golang.git
fork
clone 到本地
在项目目录下，查看远程仓库路径
git remote -v
origin  https://github.com/snoredude/golang.git (fetch)
origin  https://github.com/snoredude/golang.git (push)
只显示2行内容，说明该项目还未设置upstream （上游代码库）
设置upstream
git remote add upstream https://github.com/cncamp/golang.git
检查是否成功
git remote -v
origin  https://github.com/snoredude/golang.git (fetch)
origin  https://github.com/snoredude/golang.git (push)
upstream        https://github.com/cncamp/golang.git (fetch)
upstream        https://github.com/cncamp/golang.git (push)
检查本地代码是否修改
git status
如果本地有修改，需要先从本地仓库推送到GitHub仓库。
抓取源仓库的更新
git fetch upstream
* [new branch]      master     -> upstream/master
上游仓库的更新（commit）会本存储为本地的分支 upstream/master
切换分支
切换到需要更新合并的分支
git checkout master
如果本身就位于该分支，不需要切换
执行合并
git merge upstream/master
如有代码冲突，需要解决。
推送到Gibhub
git push origin maste
```

### 其他
```
git status
git branch -vv
git branch -v
git branch -rv
git branch -av
git checkout test
git add .
git add -A
git add filename
git commit
git branch test
git branch fix test
git checkout -b newbranch
git branch -d fix
git fetch remote
git pull remote test
git push remote名称 test
查看全局的配置信息
查看某个仓库下的配置信息
查看当前仓库的变更情况
查看所有分支的最后一个提交信息（当前代码所在分支会被高亮）
列出所有本地分支
列出所有远端分支
列出所有本地分支 + 远端分支
切换到 test 分支
把当前目录下及子目录下的所有变更都加入到暂存区
把仓库内所有变更都加入到暂存区
把指定文件加入到暂存区
把暂存区的修改保存到本地仓库 
基于当前分支创建新的 test 分支
基于 test 分支创建新的 fix 分支
创建分支并切换到该分支
删除指定分支
拉取远端的所有分支和变更  
拉取远端的某个分支的变更，然后 merge 到本地分支
推送本地分支到远端
```


## 7.7. 工作原理 / 流程
- Workspace: 工作区 ( 项目代码所在目录 )
- Index / Stage: 暂存区 ( 位于工作区的.git目录中 )
- Repository: 本地仓库 ( 代码版本库, 工作区的.git目录, 包含项目代码的第一个分支 master, 以及指向 master 的一个指针 HEAD )
- Remote: 远程仓库

### 创建本地仓库
```
cd f:\project
mkdir gitdemo
cd gitdemo
git init
new-item README.md -type file
add-content README.md -value "1"
get-content README.md 或 cat README.md  
git status
git add .\README.md ( 把文件添加到暂存区 )
git status
git commit -m "add README.md" ( 把暂存区的所有内容提交到当前分支上 )
git status
add-content README.md -value "2"
get-content README.md
git status
git diff .\README.md
git add .\README.md
git commit -m "update README.md"
add-content README.md -value "3"
git add .\README.md
git log
git reset --hard HEAD^ ( 回退到上一版本 )
git reset --hard HEAD~1
git reflog ( 查看回退日志 )
git reset --hard 3230acd ( 回退到指定版本 )
add-content README.md -value "4"
git restore .\README.md ( 弃置文件的修改)
git restore --staged .\README.md ( 撤销上次 add)

ssh-keygen -t rsa –C "caffeine.dude.go@gmail.com"
Settings > SSH keys > bai-hasee-pub-key
git remote add origin git@github.com:snoredude/gitdemo.git
git branch -M main ( 对当前的 branch 重命名)
git push -u origin main ( 把本地 master 分支内容推送到远程 master 分支，并把两个分支关联起来 )
git push origin master ( 把本地分支的 commit 推送到远程分支上)
git checkout -b dev ( 创建并切换到 dev 分支)
git branch ( 查看当前分支, 会列出所有的分支, 当前分支前面会添加一个星号 )

add-content README.md -value "4"
git add .\README.md
git commit -m "add 4 on dev"
cat README.md
git checkout main
git merge dev ( Fast-forward, 快进模式, 直接把 main 指向 dev 的当前提交，所以合并速度非常快 )
cat README.md
git branch
git branch -d dev ( 删除 dev 分支 )

查看分支：git branch
创建分支：git branch name
切换分支：git checkout name
创建+切换分支：git checkout –b name
合并某分支到当前分支：git merge name
删除分支：git branch –d name

git checkout -b dev
add-content README.md -value "5-dev"
git add .\README.md
git commit -m "add 5-dev on dev"
git checkout main
add-content README.md -value "5-main"
git add .\README.md
git commit -m "add 5-main on mian"
git merge dev

<<<<<<< HEAD
5-main
=======
5-dev
>>>>>>> dev


add .\README.md
commit -m "conflict: Accept Both Changes"
git log

git merge --no-ff -m "merge with no-ff" dev
git branch -d dev ( 此方式删除分支后, 被删除的分支信息还在 )


临时在 dev 分支上创建新的 issue 分支
add-content README.md -value "5-tmp"
git stash
git status
git checkout -b issue
修改 5-dev 为 5-dev-issue
git add .\README.md
git commit -m "fix bug"
git checkout main
git merge --no-ff -m "merge with no-ff" issue
git add .\README.md
git commit -m "merge issue"
git branch -d issue
git checkout dev
git stash list
git stash pop( git stash apply + git stash drop )

git remote ( 查看远程库的信息, 远程库的默认名称是origin )
git remote –v ( 查看远程库的详细信息 )
origin  git@github.com:snoredude/gitdemo.git (fetch) // 要抓取的远程仓库
origin  git@github.com:snoredude/gitdemo.git (push)  // 推送到的远程仓库

git push origin main ( 把本地 main 分支推送到远程仓库的 main 分支上)
git push origin dev ( 把本地 dev 分支推送到远程仓库的 dev 分支上)

git branch --set-upstream dev origin/dev
```